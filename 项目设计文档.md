# 泛型编译单态化缓存一致性服务

> 项目编码：task279 ｜ 需求编号：REQ-20260826-146 ｜ 方向：纯后端服务
> 生成方式：从 `docs/需求池.md` 备选池**原子领取** REQ 并生成健康 Go 项目基线（req-pool-codegen 工作流）。本任务**只产出可运行、可测试的健康基线环境**：不含 Bug 注入、不派生 author/FIX、不出题面、不交付业务系统。

## 1. 项目概述

泛型单态化（monomorphization）要求：对一组**类型实参与约束解**，编译器应得到**唯一稳定的单态化身份键**，从而复用同一份实例化代码。当两组实例"语义等价"却因约束序列化顺序、类型别名等不同而算出**不同缓存键**时，会产生重复代码实体或漏报分歧。

本服务面向**编译器工程师**，提供：导入泛型定义/类型实参/约束解/目标 ABI → 规范化类型图 → 计算单态化键 → 比对实例缓存 → 定位规范化分歧（冲突）→ 合并等价约束 → 发布一致性验证快照。

核心不变量：**相同(定义, 规范化实参集, 规范化约束解, ABI) ⇒ 相同单态化键**。

## 2. 领域模型与状态机

### 2.1 实体（9 张持久化表）
`abis` 目标 ABI 版本 ｜ `definitions` 泛型定义（func/type + 有序形参） ｜ `type_args` 类型实参（含可选别名 `alias_of`） ｜ `constraints` 约束解（针对一个实参集） ｜ `batches` 编译批次 ｜ `requests` 单态化实例请求 ｜ `keys` 单态化身份键（每请求唯一，upsert） ｜ `cache_entries` 实例缓存条目（每请求唯一，upsert） ｜ `snapshots` 验证快照。

### 2.2 状态机
| 实体 | 状态 |
|------|------|
| 编译批次 | `receiving` 接收中 / `normalizing` 规范化中 / `conflicted` 存在冲突 / `releasable` 可发布 / `sealed` 封存 |
| 实例请求 | `raw` 原始 / `normalized` 已规范化 / `equivalent` 等价 / `conflict` 冲突 / `excluded` 排除 |
| 缓存条目 | `candidate` 候选 / `unique` 唯一 / `duplicate` 重复 / `conflict` 分歧(同实参集同ABI异键) / `abi_mismatch` ABI不符 |
| 验证快照 | `draft` 草稿 / `published` 发布 / `superseded` 替代 |

缓存条目的 `conflict` 是"规范化分歧"的细分：同一实参集、同一 ABI，却算出不同键串——这正是 REQ 端到端场景要捕获的问题，并会把批次状态抬高到 `conflicted`。

## 3. 核心业务闭环

```
导入定义/实参/约束/ABI
   → NormalizeRequest：别名解析 + 实参集排序 + 约束解法归一 ⇒ 单态化键
   → CompareCache：与既有缓存条目比对，判定 unique/duplicate/conflict/abi_mismatch
   → 发现 conflict ⇒ 批次进入 conflicted
   → MergeAndResolve：合并等价约束重算键，消除分歧 ⇒ 批次 releasable
   → CreateSnapshot / PublishSnapshot：发布一致性证明
```

**端到端场景（smoke-test 覆盖）**：两组别名类型（IntList→List<Int> 与 直接使用 List<Int>）、约束语义等价但顺序不同（["Ord k"] 与 ["k Ord"]），初判为分歧；合并约束后归一为同一单态化身份，发布一致性快照。

## 4. 架构与目录

```
env/
├── go.mod                      (go 1.26.3 + modernc.org/sqlite v1.52.0)
├── cmd/monocache/main.go       启动 / --smoke-test 持久化自检
├── internal/
│   ├── model/                 实体 + 状态常量 + 错误 + GenID
│   ├── store/                 db 迁移 + 9 实体 CRUD（upsert 幂等）
│   ├── normalize/             类型图规范化（别名解析、实参集排序）
│   ├── merge/                 约束解合并（去重 + 排序归一）
│   ├── keymake/               单态化键计算
│   ├── cachecmp/              缓存比对判定状态机
│   ├── snapshot/              一致性报告生成
│   ├── service/               工作流编排
│   └── httpapi/               28 路由 + handlers
├── Dockerfile / benzhi.Dockerfile
└── component-versions.json
```

模块责任（贴题 REQ 模块责任）：类型模块规范化图、约束模块求解、键模块计算身份、缓存模块对比条目、快照模块发布。

## 5. API 清单（28 个，均以 /api 为前缀）

| 分组 | 方法 + 路径 | 说明 |
|------|------------|------|
| 批次 | POST /api/batches | 创建编译批次 |
| | GET /api/batches | 列出批次 |
| | GET /api/batches/{id} | 读取批次 |
| | POST /api/batches/{id}/seal | 封存批次 |
| 定义 | POST /api/definitions | 创建泛型定义 |
| | GET /api/definitions | 列出定义 |
| | GET /api/definitions/{id} | 读取定义 |
| | POST /api/definitions/{id}/args | 添加类型实参 |
| | GET /api/definitions/{id}/args | 列出类型实参 |
| 约束 | POST /api/constraints | 创建约束解 |
| | GET /api/constraints | 按定义列出约束 |
| | GET /api/constraints/{id} | 读取约束 |
| ABI | POST /api/abis | 创建 ABI |
| | GET /api/abis | 列出 ABI |
| 请求 | POST /api/requests | 创建实例请求 |
| | GET /api/requests | 按批次列出请求 |
| | GET /api/requests/{id} | 读取请求 |
| | POST /api/requests/{id}/normalize | 规范化并计算单态化键 |
| 键 | POST /api/keys | 计算/查询单态化键 |
| | GET /api/keys/{id} | 读取请求最新键 |
| 缓存 | POST /api/cache/compare | 比对缓存并返回判定 |
| | GET /api/cache | 列出缓存条目 |
| | POST /api/cache/merge | 合并等价约束并消除分歧 |
| | POST /api/conflicts/{id}/resolve | 裁决冲突 |
| 快照 | POST /api/snapshots | 创建草稿快照 |
| | POST /api/snapshots/{id}/publish | 发布快照 |
| | GET /api/snapshots | 按批次列出快照 |
| | GET /api/snapshots/{id} | 读取快照 |

## 6. 持久化与重启恢复

- 采用 `modernc.org/sqlite`（纯 Go，CGO_ENABLED=0 可编译）落地 9 张表，SQLite 单连接避免写锁竞争（`SetMaxOpenConns(1)` + `busy_timeout`）。
- `keys` 表对 `request_id` 设唯一约束，`SaveKey` 走 `ON CONFLICT(request_id) DO UPDATE` **upsert**，保证每请求仅一条最新键——这是修复"合并前后同秒并列导致读到旧键"的关键。
- `cache_entries` 对 `request_id` 同样 upsert，`CompareCache` 幂等。
- 重启后所有规范化身份、缓存判定、快照状态均可从库恢复（smoke-test 关闭并重开数据库校验）。

## 7. 验收门禁结果

| 门禁 | 命令 | 结果 |
|------|------|------|
| 构建 | `go build ./...` | 通过（exit 0） |
| 静态检查 | `go vet ./...` | 通过（exit 0） |
| 测试 | `go test ./...` | 通过（exit 0，无测试桩文件） |
| 持久化自检 | `go run ./cmd/monocache --smoke-test` | PASSED：batch=releasable, cacheEntries=2, keyLen=64 |

代码量：22 个 Go 生产文件，2540 行生产代码（不含测试与前端），超过"≥2000 行 / ≥20 文件"基线门槛。

## 8. 运行

```bash
cd env
export CGO_ENABLED=0 GOTOOLCHAIN=local
go run ./cmd/monocache --smoke-test     # 一次性持久化自检
go run ./cmd/monocache -addr :8080 -db monocache.db   # 启动长驻服务
```
